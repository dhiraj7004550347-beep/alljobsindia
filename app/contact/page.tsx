import type { Metadata } from "next";
import LegalPage from "@/components/LegalPage";
export const metadata: Metadata = { title: "Contact", description: "Contact information for All Jobs India." };
export default function ContactPage() { const email = process.env.NEXT_PUBLIC_CONTACT_EMAIL; return <LegalPage title="Contact" intro="Report an incorrect, expired or suspicious listing so it can be reviewed."><section><h2>Contact channel</h2>{email ? <p>Email us at <a href={`mailto:${email}`}>{email}</a>. Include the job URL and the official source URL.</p> : <p>A public support email has not been configured yet. The site owner should set <code>NEXT_PUBLIC_CONTACT_EMAIL</code> before launch.</p>}</section><section><h2>Recruitment questions</h2><p>We cannot accept applications, change employer deadlines or provide selection decisions. Contact the recruiting organisation through its official website for those requests.</p></section><section><h2>Do not send sensitive data</h2><p>Do not email passwords, Aadhaar numbers, banking details or application credentials.</p></section></LegalPage>; }
